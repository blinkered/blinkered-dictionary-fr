# What this evidence rests on: fr

Generated by `pnpm collections` from the committed evidence.

Every collection below was read once and its testimony written into `attestations/`, along with
the number of playable tokens it held. That total is the denominator of every rate, and recording
it is what makes the downloaded text **disposable**: a later build that adds a family reuses these
lines instead of re-reading gigabytes to rediscover them.

So the dumps are not kept. This table is the pointer back, and a rebuild that wants to re-read a
collection rather than reuse its record only has to put the file back where the path says.

| collection | tokens | what it is | where it came from |
| --- | ---: | --- | --- |
| `wiki:fr` | 1,173,847,076 |  | — |
| `gut` | 195,733,426 |  | — |
| `ia` | 82,695,385 | Internet Archive french books — literature, and the register a newspaper never reaches | [archive-fr](https://archive.org/details/booksbylanguage_french) |
| `wikisource:fr` | 13,929,245 |  | — |
| `tat` | 3,946,680 |  | — |
| `lz:fra_news_2023_1M` | 2,883,591 |  | — |
| `lz:fra_news_2024_1M` | 2,684,996 |  | — |
| `web:actualitte.com` | 832,498 |  | fetched from `actualitte.com`, recorded in `searched.tsv` |
| `web:lefigaro.fr` | 649,702 |  | fetched from `lefigaro.fr`, recorded in `searched.tsv` |
| `ebible:fraLSG` | 533,042 |  | — |
| `web:la-croix.com` | 401,204 |  | fetched from `la-croix.com`, recorded in `searched.tsv` |
| `web:lemonde.fr` | 381,199 |  | fetched from `lemonde.fr`, recorded in `searched.tsv` |
| `web:rts.ch` | 221,466 |  | fetched from `rts.ch`, recorded in `searched.tsv` |
| `web:sudouest.fr` | 179,332 |  | fetched from `sudouest.fr`, recorded in `searched.tsv` |
| `web:letemps.ch` | 178,067 |  | fetched from `letemps.ch`, recorded in `searched.tsv` |
| `web:nouvelobs.com` | 174,622 |  | fetched from `nouvelobs.com`, recorded in `searched.tsv` |
| `web:lapresse.ca` | 101,335 |  | fetched from `lapresse.ca`, recorded in `searched.tsv` |
| `web:franceinfo.fr` | 14,863 |  | fetched from `franceinfo.fr`, recorded in `searched.tsv` |
| `web:rtbf.be` | 288 |  | fetched from `rtbf.be`, recorded in `searched.tsv` |
| `web:ladepeche.fr` | 2 |  | fetched from `ladepeche.fr`, recorded in `searched.tsv` |
| `web:lesoir.be` | 0 |  | fetched from `lesoir.be`, recorded in `searched.tsv` |
| `web:rfi.fr` | 0 |  | fetched from `rfi.fr`, recorded in `searched.tsv` |

Totals are playable tokens after folding, not bytes. A collection missing from this table was
never scanned; one present with no link was fetched by hand and its path is given instead.
